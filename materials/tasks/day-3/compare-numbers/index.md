---
title: Compare Numbers
---

# Compare Numbers

## Problem Setup
- login to [CS50 IDE](https://cs50.dev)
- write `cd` in the *terminal* to go to the home directory
- write `mkdir compare-numbers` to create a folder called `compare-numbers`
- write `cd compare-numbers` to go to the `compare-numbers` folder
- write `code compare-numbers.cpp` to create a file called `compare-numbers.cpp` and open it in the editor

## Problem
Implement a program that takes two integer variables **X** and **Y** from the user and compares them:
* If $X < Y$, print `X is less than Y`
* If $X > Y$, print `X is greater than Y`
* If $X = Y$, print `X is equal to Y`

Your program should output the prompts `X = ` and `Y = ` before reading each input.

## Example

**Input:**
```
5
10
```

**Output:**
```
X = Y = X is less than Y
```

## How to Test
To test your program manually, compile it with `make` and run it:

```bash
make compare-numbers
./compare-numbers
```

To test your code automatically with `check50`, run:

```bash
check50 iti-technical-team/problemset/pset/lec/3/compare-numbers
```

## Submit Your Code
Make sure you are in the `compare-numbers` directory, then run the following command:

```bash
submit50 iti-technical-team/problemset/pset/lec/3/compare-numbers
```
